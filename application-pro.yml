spring:
  datasource:
    url: jdbc:postgresql://postgres-service:5432/peaklog
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    flyway:
      enabled: true